// import { options } from "./options";
// import { whiteScreen } from "./whiteScreen";

const HandleEvent = {
  whiteScreen() {
    // whiteScreen(() => {}, options);
  },
};

export { HandleEvent };
